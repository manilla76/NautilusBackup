<Window x:Class="ManualRamosAddon.AddFeeder"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        mc:Ignorable="d ignore"
        Height="400" Width="800"
        Title="Add a new Feeder to Control"
        DataContext="{Binding AddFeederVM, Source={StaticResource Locator}}">

    <Grid DataContext="{Binding AppVM, Source={StaticResource Locator}}">
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Width="100" Text="Feeder Number: "/>
                <TextBox Width="50" Text="{Binding NewFeeder.FeederNumber}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Width="100" Text="Oxide: "/>
                <TextBox Width="50"  Text="{Binding NewFeeder.Oxide}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Width="100" Text="Max Delta: "/>
                <TextBox Width="50" Text="{Binding NewFeeder.MaxDelta}"/>
            </StackPanel>           
        </StackPanel>
        
    </Grid>
</Window>
